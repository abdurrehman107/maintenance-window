# maintenance-window-nightly.yaml
apiVersion: maintenanceoperator.io.maintenanceoperator.io/v1alpha1
kind: MaintenanceWindow
metadata:
  name: nightly-maintenance          # 🔄 change the name to whatever fits
  namespace: default            # 🔄 or omit for “default”
spec:
  # UTC RFC-3339 timestamps
  startTime: "2025-06-19T01:00:00Z"  # 🔄 when maintenance begins
  endTime:   "2025-07-08T03:00:00Z"  # 🔄 when maintenance ends
